using UnityEngine;
using System;
using System.Text;
using NATS.Client;

public class PlayerSpawner : MonoBehaviour
{
    /*
    public GameObject playerPrefab;
    public static Transform spawnPoint;

    //public IConnection connection = Launcher.instance.GetConnection();

    public void SpawnPlayer()
    {
        EventHandler<MsgHandlerEventArgs> playerSpawn = (sender, args) =>
        {
            //Instantiate(playerPrefab, spawnPoint.position, spawnPoint.rotation);
        };
        //connection.SubscribeAsync("blitz.game.playerJoined", playerSpawn);
        //connection.Publish("blitz.game.playerJoined", Encoding.UTF8.GetBytes("Player spawned!"));
    }

    void Start()
    {
        SpawnPlayer();
    }

    void Update()
    {
        
    }*/
}
